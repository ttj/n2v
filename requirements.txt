# NNV-Python Dependencies
# Install with: pip install -r requirements.txt

# Core dependencies
numpy>=1.20.0
scipy>=1.7.0
torch>=2.0.0
cvxpy>=1.2.0
networkx>=2.6      # required by Hexatope
torchdiffeq>=0.2.3 # required by FlowODE / log-det scores

# Optional: For examples
matplotlib>=3.3.0
torchvision>=0.15.0
scikit-learn>=1.0.0  # GMMScore (n2v.probabilistic.flow) + exp3 score baselines

# Optional: For Jupyter notebooks
jupyter>=1.0.0
ipykernel>=6.0.0

# Optional: For ONNX model export/import (CompareMATLAB examples)
onnx>=1.12.0
onnxruntime>=1.12.0
# onnx2torch is installed from git submodule (see installation instructions)
# To install: pip install -e third_party/onnx2torch

# Optional: For nice tables in notebooks
pandas>=1.3.0
